{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MisalignmentFindings",
  "description": "P3D.2: the Refactored Blueprint evaluated against each Ratified Mandate objective. M-entries are FINDINGS, not decisions — they cite blueprint elements and objectives; they never propose fixes (P3D.4's job) and never adjudicate tradeoffs (the Principal's job).",
  "type": "object",
  "required": ["findings"],
  "properties": {
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["code", "title", "description", "objective_ids", "blueprint_refs"],
        "properties": {
          "code": { "enum": ["M1", "M2", "M3", "M4", "M5"] },
          "title": { "type": "string", "maxLength": 250 },
          "description": { "type": "string", "maxLength": 2000 },
          "objective_ids": { "type": "array", "minItems": 1,
                             "items": { "type": "string" },
                             "description": "Ratified Mandate objective(s) concerned" },
          "blueprint_refs": { "type": "array", "minItems": 1,
                              "items": { "type": "string" },
                              "description": "item_ids / element refs concerned; for M1 (objective unserved) cite the families searched" }
        }
      }
    }
  }
}
